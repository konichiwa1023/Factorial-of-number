import pytest

# Factorial function implementation
def factorial(n):
    if n == 0:
        return 1
    elif type(n) == int:
        return n * factorial(n - 1)
    else:
        return -1

# Test case for input 5
def test_regular():
    assert factorial(5) == 120
